---
title: "Model Evaluation & Resampling"
subtitle: "Measuring what actually generalizes"
author: "Jae Jung · IBM 6540"
date: today
format:
  revealjs:
    theme: [default, custom.scss]
    slide-number: c/t
    progress: true
    transition: fade
    transition-speed: fast
    code-line-numbers: false
    code-fold: false
    code-tools: false
    embed-resources: true
    fig-align: center
    chalkboard: false
    preview-links: auto
    width: 1280
    height: 720
execute:
  echo: true
  warning: false
  message: false
---

## Where We Are{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
**Ch. 4–8** — Data, splitting, recipes, workflows
:::

::: {.fragment .fade-in}
**Today (Ch. 9–10)** — *How do we know if any of it worked?*
:::

::: {.fragment .fade-in}
**Ch. 11+** — Comparing and tuning models
:::

::: {.notes}
Frame today as the hinge point of the course: everything before this was building a model; everything after this is improving a model. You can't improve what you can't measure.
:::

---

## The Question Underneath Every Model{.unnumbered}

<div class="ruler-rule"></div>

::: {.r-fit-text}
"Is this model actually good —
:::

::: {.r-fit-text .accent}
or did it just get lucky?"
:::

::: {.notes}
This is the thesis of both chapters. Ch 9 gives us the ruler. Ch 10 makes sure we're not measuring on a surface that's tilted in our favor.
:::

---

## Two Chapters, One Throughline{.unnumbered}

<div class="ruler-rule"></div>

:::: {.columns}

::: {.column width="48%"}
### Ch. 9
**How do we measure it?**

Metrics: RMSE, sensitivity, ROC/AUC — the instruments.
:::

::: {.column width="4%"}
:::

::: {.column width="48%"}
### Ch. 10
**How do we measure it *honestly*?**

Resampling: cross-validation — measure the same thing many times.
:::

::::

::: {.fragment}
<div class="callout-strip">You need a ruler before you can ask whether it's calibrated.</div>
:::

---

# Part 1 — Judging Model Effectiveness {background-color="#16191c"}

### TMWR Chapter 9

---

## Why Metrics, Not Vibes{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
A fitted model produces predictions. Predictions need a **number** that says how good they are.
:::

::: {.fragment .fade-in}
That number depends on the **business question** — there is no universal "best" metric.
:::

::: {.fragment .fade-in .accent-box}
A churn model that misses real churners ≠ a churn model that over-flags loyal customers. Same accuracy. Very different cost.
:::

---

## Regression Metrics{.unnumbered}

<div class="ruler-rule"></div>

```{.r}
reg_metrics <- metric_set(rmse, rsq, mae)

reg_preds |> reg_metrics(truth = order_value_next, estimate = .pred)
```

::: {.fragment}
| metric | reads as | in dollars? |
|---|---|---|
| **RMSE** | typical error size | yes — penalizes big misses |
| **R²** | % variance explained | no — unitless, 0 to 1 |
| **MAE** | typical error size | yes — robust to outliers |
:::

::: {.notes}
Emphasize: RMSE and MAE answer "how far off, on average" but RMSE punishes large misses harder because of the square. If your data has occasional huge orders, RMSE and MAE will tell different stories — that's exercise 9.3.
:::

---

## The Confusion Matrix{.unnumbered}

<div class="ruler-rule"></div>

```{.r}
conf_mat(test_preds, truth = responded, estimate = .pred_class)
```

::: {.fragment}
<div class="instrument-grid">
<div class="cell head"></div><div class="cell head">Actual: Yes</div><div class="cell head">Actual: No</div>
<div class="cell head">Pred: Yes</div><div class="cell tp">TP</div><div class="cell fp">FP</div>
<div class="cell head">Pred: No</div><div class="cell fn">FN</div><div class="cell tn">TN</div>
</div>
:::

::: {.fragment}
Every classification metric is built from these four cells.
:::

---

## Four Cells, Four Questions{.unnumbered}

<div class="ruler-rule"></div>

::: {.incremental}
- **Sensitivity** — of true responders, how many did we *catch*?
- **Specificity** — of true non-responders, how many did we correctly *skip*?
- **Precision** — of who we *targeted*, how many actually responded?
- **Accuracy** — overall, how often were we right? *(careful — see next slide)*
:::

---

## The Accuracy Trap{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment}
Direct-mail response rates: typically **3–8%**.
:::

::: {.fragment .accent-box}
A model that predicts "no" for **every single prospect** still scores 92–97% accuracy.
:::

::: {.fragment}
It is also completely useless for targeting.
:::

::: {.fragment}
**Always pair accuracy with sensitivity / specificity.**
:::

---

## ROC Curve & AUC{.unnumbered}

<div class="ruler-rule"></div>

:::: {.columns}

::: {.column width="55%"}
```{.r}
roc_curve(test_preds, 
          truth = responded, 
          .pred_yes) |> 
  autoplot()
```

A single threshold (0.5) gives one sensitivity/specificity pair.

The ROC curve sweeps **every** threshold at once.
:::

::: {.column width="45%"}
<div class="auc-scale">
<div class="tick" style="left:0%"><span>0.5</span><br><small>coin flip</small></div>
<div class="tick" style="left:50%"><span>0.75</span><br><small>quite good</small></div>
<div class="tick" style="left:100%"><span>1.0</span><br><small>perfect</small></div>
<div class="auc-bar"></div>
</div>
:::

::::

---

## Multiclass: Same Idea, More Classes{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment}
`loyalty_tier`: Bronze / Silver / Gold / Platinum
:::

::: {.fragment}
`yardstick` computes each metric **per class** (one-vs-rest), then averages.
:::

::: {.fragment .accent-box}
**Macro-average** (default): every class counts equally — Platinum isn't drowned out by Bronze, even though Bronze has far more customers.
:::

---

# Part 2 — Resampling for Evaluating Performance {background-color="#16191c"}

### TMWR Chapter 10

---

## The Problem with One Measurement{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
Section 1 gave us *a* ROC AUC. One number, from one test set.
:::

::: {.fragment .fade-in}
**Would that number change** if we'd split the data differently?
:::

::: {.fragment .fade-in .accent-box}
We don't know — unless we measure it more than once.
:::

---

## Resubstitution: The Optimism Trap{.unnumbered}

<div class="ruler-rule"></div>

What if we score the model on the data it was **trained on**?

```{.r}
train_preds <- augment(mail_fit, new_data = mail_train)
test_preds  <- augment(mail_fit, new_data = mail_test)
```

::: {.fragment}
<div class="instrument-grid two-col">
<div class="cell head">training (resubstitution)</div><div class="cell head">test (held out)</div>
<div class="cell warn">inflated — model has memorized noise</div><div class="cell ok">honest — genuinely unseen data</div>
</div>
:::

::: {.fragment}
The gap between these two **is the bias resampling exists to detect and avoid**.
:::

---

## Why Not Just Reuse the Test Set?{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
The test set should be touched **once** — at the very end.
:::

::: {.fragment .fade-in}
Use it repeatedly while comparing or tuning models, and you start **fitting to it** — same problem as resubstitution, one level up.
:::

::: {.fragment .fade-in .accent-box}
Resampling gets a trustworthy estimate using **only training data**.
:::

---

## V-Fold Cross-Validation{.unnumbered}

<div class="ruler-rule"></div>

```{.r}
mail_folds <- vfold_cv(mail_train, v = 10, strata = responded)
```

::: {.fragment}
<div class="fold-strip">
<div class="fold-row"><span>Fold 1</span><div class="bar"><div class="held" style="left:0%"></div></div></div>
<div class="fold-row"><span>Fold 2</span><div class="bar"><div class="held" style="left:10%"></div></div></div>
<div class="fold-row"><span>Fold 3</span><div class="bar"><div class="held" style="left:20%"></div></div></div>
<div class="fold-row dim"><span>⋮</span><div class="bar"></div></div>
<div class="fold-row"><span>Fold 10</span><div class="bar"><div class="held" style="left:90%"></div></div></div>
</div>
:::

::: {.fragment}
Every row is held out **exactly once**. Train on 9 folds, score on the 10th, rotate.
:::

---

## The Other Resampling Schemes{.unnumbered}

<div class="ruler-rule"></div>

| Method | Use it when... |
|---|---|
| **V-fold CV** | default choice, most marketing data |
| **Repeated CV** | small data, fold-to-fold noise is high |
| **Bootstrap** | need uncertainty estimates, very small data |
| **Validation split** | data is huge and CV is too slow |
| **Rolling origin** | time-ordered data — campaign cohorts by month |

---

## Running It: `fit_resamples()`{.unnumbered}

<div class="ruler-rule"></div>

```{.r}
cv_results <- fit_resamples(
  mail_wf,
  resamples = mail_folds,
  metrics = metric_set(accuracy, roc_auc, sens, spec),
  control = control_resamples(save_pred = TRUE)
)

collect_metrics(cv_results)
```

::: {.fragment .accent-box}
Fits the **same workflow** 10 times — once per fold — and averages the results.
:::

---

## One Test Set vs. Ten Folds{.unnumbered}

<div class="ruler-rule"></div>

<div class="instrument-grid two-col">
<div class="cell head">Single test set</div><div class="cell head">10-fold CV mean</div>
<div class="cell">one draw from a distribution</div><div class="cell ok">averages 10 draws</div>
<div class="cell">no sense of stability</div><div class="cell ok">comes with a standard error</div>
</div>

::: {.fragment}
The standard error tells you how much that estimate might **wobble** on a different split.
:::

---

## Reading Fold-to-Fold Variability{.unnumbered}

<div class="ruler-rule"></div>

```{.r}
collect_metrics(cv_results, summarize = FALSE) |>
  filter(.metric == "roc_auc") |>
  ggplot(aes(x = id, y = .estimate)) + geom_col()
```

::: {.fragment .accent-box}
Wide spread across folds → performance is **sensitive to which prospects** end up in the assessment set. Worth investigating before trusting the model.
:::

---

## Parallel Processing — A Practical Note{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
Resampling fits the **same workflow many times** — an embarrassingly parallel task.
:::

::: {.fragment .fade-in}
```{.r}
library(doParallel)
cl <- makePSOCKcluster(parallel::detectCores() - 1)
registerDoParallel(cl)
```
:::

::: {.fragment .fade-in}
`fit_resamples()`, `tune_grid()`, and friends pick this up **automatically** once a backend is registered.
:::

---

## Today, in One Line{.unnumbered}

<div class="ruler-rule"></div>

::: {.r-fit-text}
Pick the right ruler.
:::

::: {.r-fit-text .accent}
Then measure with it more than once.
:::

---

## Next Week{.unnumbered}

<div class="ruler-rule"></div>

::: {.fragment .fade-in}
**Ch. 11 — Comparing Models with Resampling**
:::

::: {.fragment .fade-in}
Now that we can measure one model honestly — how do we compare *several* models on equal footing?
:::

::: {.fragment .fade-in .accent-box}
Bring a model you've been curious about. We'll resample it.
:::
